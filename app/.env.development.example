# Overrides applied to every development build (Android, iOS, Web).
# Copy to `.env.development` for local development.

# For local development - adjust based on your setup:
# - Web/iOS Simulator: localhost or 127.0.0.1
# - Android Emulator: 10.0.2.2 (maps to host machine)
# - Physical Android Device: Your computer's IP address (e.g., 192.168.1.100)
EXPO_PUBLIC_API_URL=http://localhost:3000
